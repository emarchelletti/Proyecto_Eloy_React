import { createContext } from "react";
//El contexto es una forma en la que todos los componentes dentro de mi provider van a acceder a ciertas props

const CartContext = createContext();


export default CartContext;